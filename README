This is a web scraper written in Python using the Beautiful Soup Library.

It scrapes the Amazon store for each item in a catalog and returns its rating.  The script can be invoked in the regular way "python scraper.py". 
